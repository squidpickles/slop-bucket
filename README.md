# Slop bucket

A Firefox extension that allows one to mark a site as (AI) slop, so it can be treated with the respect it deserves on the next visit.

Ironically, coded using AI tools.

## Key features

- Mark by domain (default), host, path
- Doesn't mess with the page data; only needs URL
